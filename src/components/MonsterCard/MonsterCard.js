import './MonsterCard.css';

const MonsterCard = (props) => {
    return (
        <div className="monster-card">
            <p> This is a monster card. </p>
        </div>
    );
}

export default MonsterCard;